title: 浏览器自动化闭环 + DeepSeek DSpark + 记忆写入 + 审美基线
summary: v1.1.8 - M51-M55: browser headless + session isolation + downloads/logs, DeepSeek DSpark family, memory_write dual-layer, design baseline
- 浏览器无头模式：自动化跑批不带 UI，CDP 截图/求值照常；无头会话拒绝 handoff
- 会话隔离 profile：每个会话独立浏览器（登录态/下载互不串），下载进独立目录不落个人下载夹
- 下载目录感知（downloads 动作）：文件真实落盘即见，下载中/完成一目了然
- 浏览器日志持久化（logs 动作）：控制台/网络/错误日志落盘 browser-logs，重启不丢
- DeepSeek DSpark 家族接入：官方 V4 已自动启用推理加速；自托管 DSpark 端点获得 reasoning 回显 + usage 计量
- memory_write 记忆写入 + 双层 MEMORY.md：用户层/项目层常驻上下文，去重封顶原子写，手写内容保留
- 内置审美基线：系统提示词常驻设计检查（层次/间距/对比度/一致性/材质感），不依赖 skill
- 全量回归：Rust 1255 / clippy 0 / vitest 554；真实 Edge/WPS 冒烟通过
